name: terraform pipeline

on:
    pull_request:
        branches:
        - staging
        - production

env:
    AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY }}
    AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_KEY }}   

jobs:
    terraform:
        name: Terraform Plan and Apply
        runs-on: ubuntu-latest
        
        steps:
            - name: Checkout code
              uses: actions/checkout@v5

            - name: Set up Terraform
              uses: hashicorp/setup-terraform@v2
              with:
                terraform_version: 1.5.7

            - name: Terraform Init
              working-directory: terraform
              run: terraform init -backend-config=env/backend-${{ github.base_ref }}.tfvars

            - name: Terraform Validate
              working-directory: terraform
              run: terraform validate

            - name: Terraform Plan
              working-directory: terraform
              run: terraform plan -var-file=env/${{ github.base_ref }}.tfvars

            - name: Terraform Apply
              working-directory: terraform
              if: github.event_name == 'pull_request' && github.event.action == 'opened' #checks that it is a pull request, and the PR is a new one
              #run: terraform destroy -auto-approve -var-file=env/${{ github.base_ref }}.tfvars 
              run: terraform apply -auto-approve -var-file=env/${{ github.base_ref }}.tfvars 